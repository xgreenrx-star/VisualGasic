' Minimal VGForm for testing
Option Explicit

Sub Form_Load()
    If Engine.is_editor_hint() Then Exit Sub
    
    ' Position at top-left (0, 0)
    Me.position = Vector2(0, 0)
    
    Print "VGForm loaded and positioned at (0,0)"
End Sub
