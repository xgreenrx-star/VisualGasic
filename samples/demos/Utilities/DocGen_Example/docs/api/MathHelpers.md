# MathHelpers

**Type:** Module &nbsp;|&nbsp; **File:** `res://MathHelpers.vg`

MathHelpers — Utility functions for common math operations.

A collection of pure functions with no side effects.
Safe to call from any module.

---

## Constants

| Name | Type | Value | Description |
|------|------|-------|-------------|
| `PI` | Double | `3.14159265358979` | The ratio of a circle's circumference to its diameter. |
| `E` | Double | `2.71828182845905` | Euler's number — base of the natural logarithm. |

## Functions

### `Clamp`

```vb
Function Clamp(value As Double, lo As Double, hi As Double) As Double
```

Clamps a value between a minimum and maximum.

**Parameters:**

| Name | Description |
|------|-------------|
| `value` | The input value |
| `lo` | The minimum allowed value |
| `hi` | The maximum allowed value |

**Returns:** The clamped result (lo ≤ result ≤ hi)

**Example:**
```vb
Dim health As Integer
health = Clamp(health - damage, 0, 100)
```

### `Lerp`

```vb
Function Lerp(a As Double, b As Double, t As Double) As Double
```

Linearly interpolates between two values.

**Parameters:**

| Name | Description |
|------|-------------|
| `a` | Start value (returned when t = 0) |
| `b` | End value (returned when t = 1) |
| `t` | Interpolation factor (0.0 to 1.0) |

**Returns:** The interpolated value

**Example:**
```vb
Dim halfway As Double
halfway = Lerp(0, 100, 0.5)   ' → 50.0
```

### `InverseLerp`

```vb
Function InverseLerp(a As Double, b As Double, value As Double) As Double
```

Returns the inverse lerp — what fraction of the way value is between a and b.

**Parameters:**

| Name | Description |
|------|-------------|
| `a` | Range start |
| `b` | Range end |
| `value` | The value to query |

**Returns:** A factor where 0.0 means value=a and 1.0 means value=b

### `Remap`

```vb
Function Remap(value As Double, fromLo As Double, fromHi As Double, toLo As Double, toHi As Double) As Double
```

Remaps a value from one range to another.

**Parameters:**

| Name | Description |
|------|-------------|
| `value` | The input value |
| `fromLo` | Source range minimum |
| `fromHi` | Source range maximum |
| `toLo` | Target range minimum |
| `toHi` | Target range maximum |

**Returns:** The remapped value

**Example:**
```vb
' Convert a 0–255 byte to a 0.0–1.0 float
Dim f As Double
f = Remap(128, 0, 255, 0.0, 1.0)   ' → ~0.502
```

### `Max`

```vb
Function Max(a As Double, b As Double) As Double
```

Returns the larger of two values.

**Parameters:**

| Name | Description |
|------|-------------|
| `a` | First value |
| `b` | Second value |

**Returns:** The greater of a and b

### `Min`

```vb
Function Min(a As Double, b As Double) As Double
```

Returns the smaller of two values.

**Parameters:**

| Name | Description |
|------|-------------|
| `a` | First value |
| `b` | Second value |

**Returns:** The lesser of a and b

### `Sign`

```vb
Function Sign(value As Double) As Integer
```

Returns the sign of a number: -1, 0, or +1.

**Parameters:**

| Name | Description |
|------|-------------|
| `value` | The number to test |

**Returns:** -1 if negative, 0 if zero, +1 if positive

### `DegToRad`

```vb
Function DegToRad(degrees As Double) As Double
```

Converts degrees to radians.

**Parameters:**

| Name | Description |
|------|-------------|
| `degrees` | Angle in degrees |

**Returns:** Angle in radians

### `RadToDeg`

```vb
Function RadToDeg(radians As Double) As Double
```

Converts radians to degrees.

**Parameters:**

| Name | Description |
|------|-------------|
| `radians` | Angle in radians |

**Returns:** Angle in degrees

### `Distance2D`

```vb
Function Distance2D(x1 As Double, y1 As Double, x2 As Double, y2 As Double) As Double
```

Computes the distance between two 2D points.

**Parameters:**

| Name | Description |
|------|-------------|
| `x1` | X of point 1 |
| `y1` | Y of point 1 |
| `x2` | X of point 2 |
| `y2` | Y of point 2 |

**Returns:** The Euclidean distance

**Example:**
```vb
Dim d As Double
d = Distance2D(0, 0, 3, 4)   ' → 5.0
```

### `WrapIndex`

```vb
Function WrapIndex(value As Integer, maxVal As Integer) As Integer
```

Wraps an integer value into a range [0, maxVal).

Useful for array index wrapping.

**Parameters:**

| Name | Description |
|------|-------------|
| `value` | The input integer |
| `maxVal` | The exclusive upper bound |

**Returns:** The wrapped value in [0, maxVal)

**Example:**
```vb
Dim idx As Integer
idx = WrapIndex(-1, 10)   ' → 9
idx = WrapIndex(10, 10)   ' → 0
```

---
*Generated by VisualGasic Documentation Generator*
