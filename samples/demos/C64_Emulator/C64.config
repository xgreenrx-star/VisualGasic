# C64 Emulator Configuration
# Lines starting with # are comments.

# Skip RAMTAS RAM test during boot?
# True  = Jump past the lengthy RAM test, reach BASIC prompt immediately.
# False = Authentic boot sequence (real C64 always runs RAMTAS on cold start).
SKIP_RAM_TEST=True
