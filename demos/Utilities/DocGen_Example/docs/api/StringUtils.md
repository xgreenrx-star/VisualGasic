# StringUtils

**Type:** Module &nbsp;|&nbsp; **File:** `res://StringUtils.vg`

StringUtils — Common string manipulation helpers.

Provides trim, pad, split, and formatting functions that
complement the built-in VB string functions.

---

## Constants

| Name | Type | Value | Description |
|------|------|-------|-------------|
| `DEFAULT_PAD_CHAR` | String | `" "` | The default padding character used by PadLeft / PadRight. |

## Enums

### TextAlign

Text alignment options for the PadToWidth function.

| Member | Value |
|--------|-------|
| `AlignLeft` | 0 |
| `AlignCenter` | 1 |
| `AlignRight` | 2 |

## Types

### TemplateVar

A key-value pair used by the template engine.

| Field | Type |
|-------|------|
| `Key` | String |
| `Value` | String |

## Functions

### `PadLeft`

```vb
Function PadLeft(s As String, width As Integer, padCh As String) As String
```

Pads a string on the left to reach a desired width.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The input string |
| `width` | The target character width |
| `padCh` | The character to pad with (default " ") |

**Returns:** The padded string

**Example:**
```vb
Print PadLeft("42", 5, "0")   ' → "00042"
```

### `PadRight`

```vb
Function PadRight(s As String, width As Integer, padCh As String) As String
```

Pads a string on the right to reach a desired width.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The input string |
| `width` | The target character width |
| `padCh` | The character to pad with (default " ") |

**Returns:** The padded string

### `CenterString`

```vb
Function CenterString(s As String, width As Integer) As String
```

Centers a string within a given width.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The input string |
| `width` | The total character width |

**Returns:** The centered string padded with spaces on both sides

### `StartsWith`

```vb
Function StartsWith(s As String, prefix As String) As Boolean
```

Returns True if the string starts with the specified prefix.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The string to test |
| `prefix` | The prefix to look for |

**Returns:** True if s begins with prefix (case-sensitive)

### `EndsWith`

```vb
Function EndsWith(s As String, suffix As String) As Boolean
```

Returns True if the string ends with the specified suffix.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The string to test |
| `suffix` | The suffix to look for |

**Returns:** True if s ends with suffix (case-sensitive)

### `RepeatString`

```vb
Function RepeatString(s As String, count As Integer) As String
```

Repeats a string a given number of times.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The string to repeat |
| `count` | How many times to repeat it |

**Returns:** The concatenated result

**Example:**
```vb
Print RepeatString("ab", 3)   ' → "ababab"
```

### `ReverseString`

```vb
Function ReverseString(s As String) As String
```

Reverses a string character-by-character.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The input string |

**Returns:** The reversed string

**Example:**
```vb
Print ReverseString("Hello")   ' → "olleH"
```

### `CountOccurrences`

```vb
Function CountOccurrences(s As String, substr As String) As Integer
```

Counts how many times a substring appears in a string.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The string to search in |
| `substr` | The substring to count |

**Returns:** The number of non-overlapping occurrences

### `ExpandTemplate`

```vb
Function ExpandTemplate(template As String, vars() As TemplateVar, varCount As Integer) As String
```

Performs simple template substitution: replaces \{\{key\}\} with value.

**Parameters:**

| Name | Description |
|------|-------------|
| `template` | The template string containing \{\{placeholders\}\} |
| `vars` | Array of TemplateVar with Key/Value pairs |
| `varCount` | Number of entries in the vars array |

**Returns:** The expanded string

**Example:**
```vb
Dim v(1) As TemplateVar
v(0).Key = "name" : v(0).Value = "World"
Print ExpandTemplate("Hello {{name}}!", v, 1)   ' → "Hello World!"
```

### `Truncate`

```vb
Function Truncate(s As String, maxLen As Integer) As String
```

Truncates a string to a maximum length, adding "..." if truncated.

**Parameters:**

| Name | Description |
|------|-------------|
| `s` | The input string |
| `maxLen` | Maximum allowed length (including ellipsis) |

**Returns:** The possibly-truncated string

---
*Generated by VisualGasic Documentation Generator*
