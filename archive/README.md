# Archive

Local-only storage for material that is **not** part of the Visual Gasic product tree.

| Path | Purpose |
|------|---------|
| [`examples_mirror/`](examples_mirror/) | Former top-level `examples/` directory (~1.2 GB accidental repo mirror; 0 git-tracked files). Kept locally for reference; not shipped in releases. |

Do not add new product code here. Prefer `samples/` for games, apps, and demos.
