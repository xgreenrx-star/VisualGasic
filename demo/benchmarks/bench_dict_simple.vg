Function BenchDictSimple(ByVal iterations As Long) As Long
    Dim dict As Dictionary
    Dim i As Long
    Dim sum As Long
    
    Set dict = New Dictionary
    dict("a") = 0
    dict("b") = 0
    dict("c") = 0
    dict("d") = 0
    dict("e") = 0
    
    For i = 0 To iterations - 1 Step 1
        dict("a") = i
        dict("b") = i + 1
        dict("c") = i + 2
        dict("d") = i + 3
        dict("e") = i + 4
        sum = sum + i
    Next i
    
    BenchDictSimple = sum
End Function
