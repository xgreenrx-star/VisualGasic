Sub Main()
    Dim a = 626.8
    Dim b = 628.0
    
    If a > b Then
        Print "FAILURE: " & a & " > " & b & " is True!"
    Else
        Print "SUCCESS: " & a & " > " & b & " is False."
    End If
End Sub
