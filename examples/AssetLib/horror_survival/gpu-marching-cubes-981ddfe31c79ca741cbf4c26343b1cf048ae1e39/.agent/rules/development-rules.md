---
trigger: always_on
---

We are using Windows 10 python shell for everything, never use powershell comands or cmd command line prompt.
Always list the options, so that answer can be single letters or numbers.
Always add prefix when printing debug. If testing specific feature/bug then make unique prefix to filter the specific feature/bug debug log related messages to resolve issue. Always look for root causes, extreme caution on need of patching instead of real solution.