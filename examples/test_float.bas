Sub Main()
    Dim 626.8 As Variant
    Dim 628.0 As Variant
    
    If a > b Then
        Print "FAILURE: " & a & " > " & b & " is True!"
    Else
        Print "SUCCESS: " & a & " > " & b & " is False."
    End If
End Sub
