Build a playable 2D Asteroids clone under ai_projects.

Requirements:
- Node2D main scene with a .vg script attached (not a Form Designer form)
- The .vg script must implement _Ready, _Process(delta), and _Draw using DrawRect, DrawCircle, and DrawString
- Arrow keys rotate the ship; Up thrusts forward; Space shoots bullets
- Screen wrap on all edges
- Start with 4–6 drifting asteroids; when hit, split into smaller pieces (at least one split level)
- Show score with DrawString in a corner
- Set main_scene so the AI Pair ▶ Run button works immediately

Reply with one fenced ```vg-project-spec``` JSON block only. Include project_name, subdir "ai_projects", main_scene, and files[] with the complete .vg game source (not TODO stubs). Keep ≤ 6 files total.
